# SPDX-License-Identifier: Apache-2.0
"""cve_rem.kill_switches — governance pack entry-point marker.

Loaded via the plugin registry by id ``cve_rem.kill_switches``. Pack
contents (manifest.yaml + rules.clp) live in this directory. JWT
signing happens at deploy time via the krakntrust signing pipeline,
not at scaffold time.
"""
