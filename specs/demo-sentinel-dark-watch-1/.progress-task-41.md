## Completed Tasks
- [x] 1.42 Create `scripts/train_detector.py` — pending commit

## Current Task
Awaiting next task

## Learnings
- ModelRegistry is async (aiosqlite-backed), so CLI scripts use asyncio.run() for registration
- Registry `register()` and `alias()` API requires bootstrap() first, close() after
